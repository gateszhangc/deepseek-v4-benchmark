apiVersion: v1
kind: Service
metadata:
  name: deepseek-v4-benchmark
  labels:
    app.kubernetes.io/name: deepseek-v4-benchmark
    app.kubernetes.io/part-of: deepseek-v4-benchmark
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: deepseek-v4-benchmark
    app.kubernetes.io/part-of: deepseek-v4-benchmark
  ports:
    - name: http
      port: 80
      targetPort: http
